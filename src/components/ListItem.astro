---
interface Props {
  href?: string;
  header?: boolean;
}

const { href = "", header = false } = Astro.props;
---

<li class={header ? "header" : ""}>
  {
    href === "" ? (
      <slot />
    ) : (
      <a href={href}>
        <slot />
      </a>
    )
  }
</li>

<style>
  li {
    all: unset;
    display: block;
    border-top: 1px solid #ddd;
    padding-inline: 1rem;
    padding-block: 0.5rem;
  }

  li.header {
    background-color: #ddd;
    font-weight: bold;
  }

  li:first-child {
    border: none;
  }

  a {
    color: royalblue;
    text-decoration: none;
  }
</style>
